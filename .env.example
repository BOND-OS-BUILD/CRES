# Enables real email delivery for the contact form (via https://resend.com).
# Without this, form submissions are only logged to the server console.
RESEND_API_KEY=

# Optional: the verified "from" address once you've set up a sending domain
# in Resend. Defaults to Resend's shared test address if omitted.
RESEND_FROM_EMAIL=
